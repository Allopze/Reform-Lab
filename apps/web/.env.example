# ── Reform Lab — Frontend Environment Variables ──
# Use this only if you run apps/web by itself.
# For normal full-stack development, the root .env is the canonical source.

NEXT_PUBLIC_API_URL=http://localhost:4040
