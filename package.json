{
  "name": "reform-lab",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "concurrently -n web,api -c cyan,green \"npm run dev:web\" \"npm run dev:api\"",
    "dev:web": "cd apps/web && npm run dev",
    "dev:api": "cd apps/api && ENV_FILE=../../.env go run ./cmd/server"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
